var config = {};
var env = process.env.NODE_ENV || 'development';

//change this to your production settings
if (env === 'production'){
	config.host = '127.0.0.1';
	config.port = 8001;
}

else{
	config.host = '127.0.0.1';
	config.port = 3000;
}



module.exports = config;